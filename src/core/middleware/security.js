import { AppError } from '../../shared/errors.js';
export const cors = ({ origins, production }) => (req, res, next) => { const origin = req.get('origin'); const local = !production && /^https?:\/\/(localhost|127\.0\.0\.1)(:\d+)?$/.test(origin || ''); if (origin && !origins.includes(origin) && !local) return next(new AppError(403, 'CORS_ORIGIN_DENIED', 'Origin is not allowed')); if (origin) { res.set('access-control-allow-origin', origin); res.set('vary', 'Origin'); res.set('access-control-allow-credentials', 'true'); res.set('access-control-allow-headers', 'authorization,content-type,x-request-id'); res.set('access-control-allow-methods', 'GET,POST,PUT,PATCH,DELETE,OPTIONS'); } if (req.method === 'OPTIONS') return res.sendStatus(204); next(); };
